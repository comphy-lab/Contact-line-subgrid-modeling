name: Solver tests

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  standalone-c99:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and test
        run: make test
